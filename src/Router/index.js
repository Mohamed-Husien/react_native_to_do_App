import { NavigationContainer } from "@react-navigation/native"

export const PATH = {
    HOME: "Home",
}
const index = () => {
    return (
        <NavigationContainer>

        </NavigationContainer>
    )
}

export default index

